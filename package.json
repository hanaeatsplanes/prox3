{
	"name": "prox3",
	"version": "1.0.0",
	"type": "module",
	"main": "src/index.ts",
	"scripts": {
		"build": "bun build . --minify --outdir=dist --target=bun --footer='// gay gay homosexual gay :3'",
		"compile": "bun build . --compile --outfile=dist/app",
		"start": "bun run dist/src/index.js",
		"ci": "biome check . --error-on-warnings",
		"lint": "biome check --fix && biome format --write",
		"lint:check": "biome check && biome format",
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "bun run --watch src/index.ts"
	},
	"dependencies": {
		"elysia": "^1.4.25"
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.2",
		"@types/bun": "^1.3.9",
		"@types/node": "^25.2.3"
	}
}
